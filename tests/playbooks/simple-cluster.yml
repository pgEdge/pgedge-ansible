---

- hosts: pgedge
  any_errors_fatal: true

  collections:
  - pgedge.platform

  tasks:
  - include_role:
      name: init_server

  - include_role:
      name: install_repos

  - include_tasks: switch-repo.yml

  - include_role:
      name: install_pgedge

  - include_role:
      name: setup_postgres

  - include_role:
      name: setup_pgedge
