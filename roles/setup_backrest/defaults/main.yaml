---

backup_repo_params: {}

# Retention, which this role renders into pgbackrest.conf. The backup schedule
# that used to sit beside these now belongs to finalize_backrest, which installs
# the cron entries: a role's defaults are visible only to that role, so the
# schedule has to live with the tasks that read it.

full_backup_count: 1
diff_backup_count: 6
