---

cluster_name: demo
repo_name: release
repo_prefix: ""
zone: 1
pg_version: 17
spock_version: "5.0.4"
tls_validity_days: 3560

db_names:
- demo
db_user: admin
db_password: secret

pgedge_user: pgedge
pgedge_password: secret

replication_user: replicator
replication_password: secret

custom_hba_rules: []

# pgBouncer connection pooler. Pooling is opt-in by inventory group: a node
# runs a pooler when it is a member of the 'pgbouncer' group, which must be a
# subset of the 'pgedge' group. Nodes outside that group are untouched.
#
# pgEdge's pgBouncer from the pgEdge repo. >=1.21 so it supports
# max_prepared_statements for transaction pooling.
pgbouncer_package: pgedge-pgbouncer
pgbouncer_port: 6432
pgbouncer_listen_addr: "*"
# session = transparent (safe with apps that use prepared statements, e.g.
# Mattermost); switch to "transaction" for real connection pooling, but then
# either use pgBouncer >= 1.21 with pgbouncer_max_prepared_statements > 0 or
# add "default_query_exec_mode=simple_protocol" to the client's connection.
pgbouncer_pool_mode: session
pgbouncer_max_client_conn: 1000
pgbouncer_default_pool_size: 25
pgbouncer_max_prepared_statements: 0
# pgBouncer keeps no list of Postgres passwords. It logs in as this role and
# calls a SECURITY DEFINER lookup to fetch the requested user's stored SCRAM
# verifier, so any Postgres role can use the pooled endpoint — including roles
# created long after deployment. setup_postgres creates the role and the lookup
# wherever the zone pools; this password is the only one written to disk, in
# pgBouncer's auth_file, because the admin console cannot use the lookup.
pgbouncer_auth_user: pgbouncer_auth
pgbouncer_auth_password: secret
# Client TLS on the pooled endpoint. Postgres already presents a certificate on
# every node — setup_postgres issues one and turns ssl on — so a pooled client
# should not have to drop to cleartext to use the pooler. "allow" accepts
# exactly what the direct endpoint accepts today: TLS when the client asks for
# it, plaintext when it does not, so no existing client changes behaviour. Set
# "require" to make TLS mandatory on the pooled endpoint, or "disable" to turn
# it off and omit the certificate entirely. verify-ca and verify-full are not
# supported: they demand a client certificate, which needs the pg_hba option
# syntax pgBouncer cannot parse.
pgbouncer_client_tls_sslmode: allow
# Where the pooler's certificate comes from: the controller-side staging
# directory setup_postgres generates into, which is also where PGDATA's copy
# came from — so the pooled and direct endpoints present the same certificate,
# and replacing this replaces both. Deliberately not read from PGDATA on the
# node: on an HA replica that copy arrives with Patroni's clone, which has not
# necessarily finished when this role runs.
pgbouncer_tls_cert_source: tls/postgres/server.crt
pgbouncer_tls_key_source: tls/postgres/server.key
# Extra client-authentication rules for the pooled endpoint, in the same shape
# as custom_hba_rules. The pooler enforces its own HBA file: it admits the
# cluster's own nodes, this zone's proxies and the node's loopback, and nothing
# else, so an application reaching the pooler across the network has to be named
# somewhere. Naming it in custom_hba_rules admits it to both endpoints; naming
# it here admits it to the pooled endpoint alone, which is the narrower grant
# and usually the one that was meant.
pgbouncer_hba_rules: []

is_ha_cluster: false
exception_behaviour: "transdiscard"

proxy_port: 5432
proxy_node: ''

# The proxy-layer port for the pooled listener, mirroring how proxy_port fronts
# pg_port: pooler_port fronts each pooled node's pgbouncer_port. It is a
# separate setting because HAProxy may share a host with Postgres and pgBouncer
# (see docs/configuration/proxy.md), and identical values collide there.
# Nothing is emitted on this port unless the zone has a node in the 'pgbouncer'
# group.
pooler_port: 6432

pg_port: 5432

pg_home: >-
  {{ 
    "/var/lib/pgsql" if ansible_facts['os_family'] == "RedHat" 
    else
    "/var/lib/postgresql" if ansible_facts['os_family'] == 'Debian'
  }}

pg_path: >-
  {{
    "/usr/pgsql-" + pg_version|string if ansible_facts['os_family'] == "RedHat"
     else
    "/usr/lib/postgresql/" + pg_version|string if ansible_facts['os_family'] == 'Debian'
  }}

pg_data: >-
  {{ 
    pg_home + "/" + pg_version|string + "/data" if ansible_facts['os_family'] == "RedHat"
    else
    pg_home + "/" + pg_version|string + "/" + cluster_name if ansible_facts['os_family'] == 'Debian' 
  }}

backup_host: ""
backup_user: backrest
backup_password: secret

backup_repo_type: ssh
backup_repo_user: "{{ ansible_user_id }}"
backup_repo_path: "/home/{{ ansible_user_id }}"

patroni_tls_dir: "/etc/patroni/tls"

# Patroni cluster name within the DCS. This is the second half of every key
# Patroni writes, and the collection derives it from values that are identical
# on every node. A store shared by more than one zone needs a distinct scope
# per zone so the zones do not claim the same keys.

patroni_scope: "{{ pg_version }}-{{ cluster_name }}"

# Key prefix Patroni uses within the DCS, forming the first half of every key.
# The collection-managed etcd cluster is deployed per zone, so every zone can
# safely share this default. A store that spans zones needs each zone to write
# to a distinct key, which patroni_scope usually provides; change this instead
# when the prefix itself must differ, such as when the store grants access per
# prefix.

patroni_namespace: /db/

patroni_dcs:
  type: etcd3
  # parameters: intentionally omitted to trigger default substitution
