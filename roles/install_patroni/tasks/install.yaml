---

- name: Ensure Patroni TLS directory exists owned by the postgres user
  file:
    path: "{{ patroni_tls_dir }}"
    state: directory
    owner: postgres
    group: postgres
    mode: 0700
  become: true
