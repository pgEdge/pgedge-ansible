tls
host-keys
.claude/

# Generated by run-test.sh so the containers accept the harness's key.
tests/.ssh/
tests/docker/authorized_keys

# Built by the Makefile from galaxy.template.yml.
galaxy.yml
pgedge-platform-*.tar.gz
